上次执行结果的： 
 result_<TASK_ID>.json 全文 ✅ 
 
 run_<TASK_ID>.log 两段： 
 
 HEAD 60（前 60 行） 
 
 TAIL 200（后 200 行） 
 这样首错/上下文基本都能抓到 ✅ 
 
 git_diff_<TASK_ID>.patch： 
 
 如果 ≤ 800 行：全文发 ✅ 
 
 如果 > 800 行：发 文件级摘要（每个文件：新增/删除行数）+ 关键文件全文 diff（比如 src/... 里最核心的 1~3 个文件）✅ 
 
 deliverables_index.json（一个索引清单，列出本次 bundle 里有哪些文件、大小、hash）✅ 
 
 LATEST.json 内容（或 task_id + git_ref 也行）✅